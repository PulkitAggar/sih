import 'package:flutter/material.dart';

class SreenToDisplay extends StatelessWidget {
  const SreenToDisplay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
